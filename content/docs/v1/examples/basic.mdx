---
title: Basic Usage
description: Small end-to-end example using the client.
---

# Basic Usage

```ts
import { Client } from 'fandom.js';

const client = new Client({ site: 'minecraft.fandom.com' });
const result = await client.search.query({ q: 'Creeper', limit: 1 });
console.log(result[0]);
```