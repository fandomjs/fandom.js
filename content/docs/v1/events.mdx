---
title: Events
description: Real-time and reactive patterns using EventManager.
---

# Events

Subscribe to site events for reactive workflows.

```ts
const sub = client.events.subscribe({ type: 'recentchanges' }, (evt) => {
  console.log('Change', evt);
});

// later
sub.unsubscribe();
```

See ./enums for event types.