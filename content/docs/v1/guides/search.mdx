---
title: Search Guide
description: Query pages and content with the SearchManager.
---

# Search Guide

```ts
const results = await client.search.query({ q: 'Creeper', limit: 5 });
for (const r of results) {
  console.log(r.title, r.url);
}
```