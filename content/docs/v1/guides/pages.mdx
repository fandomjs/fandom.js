---
title: Pages Guide
description: Create, read, update pages safely and efficiently.
---

# Pages Guide

```ts
const page = await client.pages.getByTitle('Creeper');
const text = await client.pages.getWikitext(page.id);
```